import ObjectBase from 'internal/object/object-base';
import NVG from '../../../nanovg-webgl';

export default class Paper extends ObjectBase {

  constructor() {
    super();
  }

  draw(vg: NVG, timecode: number): void {
    
  }
}